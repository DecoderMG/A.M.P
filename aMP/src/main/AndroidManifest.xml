<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" >

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AMP"
        android:largeHeap="true">
        <activity
            android:name="com.dmgproductions.amp.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AMP"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name="com.dmgproductions.amp.gestures.GestureRecognitionService"
            android:enabled="true"
            android:exported="false"
            android:label="GestureRecognitionService" >
            <intent-filter>
                <action android:name="com.dmgproductions.amp.gestures.GESTURE_RECOGNIZER" />
            </intent-filter>
        </service>

    </application>

</manifest>
